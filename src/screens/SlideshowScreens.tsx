import * as React from 'react';
import { Component } from 'react';


export interface SlideshowScreenProps {
    
}
 
export interface SlideshowScreenState {
    
}
 
class SlideshowScreen extends React.Component<SlideshowScreenProps, SlideshowScreenState> {
    constructor(props: SlideshowScreenProps) {
        super(props);
        this.state = { :  };
    }
    render() { 
        return (  );
    }
}
 
export default SlideshowScreen;